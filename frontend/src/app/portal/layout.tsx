import { CustomerPortalLayout } from "@/components/portal/PortalRouteLayout";

export default function PortalLayout({ children }: { children: React.ReactNode }) {
  return <CustomerPortalLayout>{children}</CustomerPortalLayout>;
}
