name: Deploy Frontend Preview

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  vercel-preview:
    name: Vercel Build Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: |
            frontend/package-lock.json
            backend/package-lock.json
      - run: npm ci --prefix ../backend
      - run: npm run vercel:build --prefix ../backend
      - run: npm ci
      - run: npm run build
        env:
          NEXT_PUBLIC_SITE_URL: https://green-rock-website.vercel.app

  # Connect Vercel to GitHub for automatic production deploys:
  # vercel.com → Import Project → root directory: frontend
